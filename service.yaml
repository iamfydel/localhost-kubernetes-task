apiVersion: v1
kind: Service
metadata:
  name: building-apps-svc
  labels:
    app: devops-accelerate
spec:
  selector:
    app: devops-accelerate
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
